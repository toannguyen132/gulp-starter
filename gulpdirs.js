var BUILD = 'build';
var BUILD_CSS = 'build/css';
var BUILD_JS = 'build/js';
var BUILD_FONTS = 'build/fonts';
var BUILD_IMG = 'build/img';

module.exports = {
  base: BUILD,
  css: BUILD_CSS,
  js: BUILD_JS,
  fonts: BUILD_FONTS,
  img: BUILD_IMG
}
